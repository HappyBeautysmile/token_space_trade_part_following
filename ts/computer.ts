export class Computer {
    private ctx = document.createElement("CANVAS");

    createGreenBars() {
        // this.ctx.moveTo(0, 0);
        // this.ctx.lineTo(200, 100);
        // this.ctx.stroke();
    }
}